<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Training"
        tools:targetApi="31">
        <activity
            android:name=".CreateDivision"
            android:exported="false" />
        <activity
            android:name=".AddExerciseActivity"
            android:exported="false" />
        <activity
            android:name=".DivisionActivity"
            android:exported="false" />
        <activity
            android:name=".CreateWorkout"
            android:exported="false" />
        <activity
            android:name=".CreateExercise"
            android:exported="true"
            android:label="@string/title_activity_create_exercise"
            android:theme="@style/Theme.Training" />
        <activity
            android:name=".ExerciseActivity"
            android:exported="false" />
        <activity
            android:name=".CalendarActivity"
            android:exported="false"
            android:theme="@style/Theme.Training" />
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Training">

            <!-- Removendo android:label para ocultar o nome do ícone de lançamento -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>